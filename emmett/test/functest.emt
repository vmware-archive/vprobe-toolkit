void function(int a, int b);
void noArgs();
void vafunc(int a, ...);
int returnsInt();
string returnsStr(int snap, ...);

int foo;
string nameStr;

string
name(string str, int arg)
{
   sprintf(str, "fooble: %d\n", arg);
   return str;
}  

void
voidFunc(int arg)
{
  printf("void function with an arg!\n");
}

void
voidFunc2()
{
   printf("void function with no args\n");
}

MyFunc {
   foo = returnsInt();
   function(foo, 2*1+3*bar(13));
   vafunc(foo);
   vafunc(foo, "args are fun!\n");
   vafunc(foo, "args are fun",
          "-ctional\n");
   name(nameStr, 12);
   printf("%s\n", nameStr);
   voidFunc(1);
   voidFunc2();
}
